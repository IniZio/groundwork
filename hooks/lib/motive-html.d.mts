// Type declarations for motive-html.mjs

/**
 * Render a compiled motive view as a self-contained HTML string.
 * Pure function — same input always yields the same output.
 */
export declare function renderHtml(view: object | null | undefined): string
