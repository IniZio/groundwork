---
name: bdd-implement
description: Alias for `implement`. Retained for backwards compatibility — load `implement` instead.
---

# bdd-implement → implement

This skill has been renamed to `implement`. Load the `implement` skill instead:

```
skill: implement
```

The `implement` skill leads with vertical-slice decomposition and maximum coder fan-out, with behavioral validation as the validation approach — not the headline.
