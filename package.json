{
  "name": "groundwork",
  "version": "2.1.1",
  "description": "groundwork v2 — glue that adapts to per-repo conventions and enforces best-practice direction",
  "type": "module",
  "bin": {
    "gw": "src/cli/main.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "gw": "bun src/cli/main.ts"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/bun": "latest"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
