{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "spec-concept.schema.json",
  "$comment": "Deliberately NOT covered by this schema (handled by spec-lint.mjs at runtime):\n  1. summary-length: ≤25-word limit — JSON Schema has no word-count keyword.\n  2. origin_decision_ref referential integrity: whether the value resolves to a real decision in a motive's Decision Log.\n  3. superseded_by referential integrity: whether the target node exists.\n  4. snapshot_of referential integrity: whether the referenced snapshot exists.\n  5. parent-agrees-with-directory-path: the parent field value must match the concept's containing directory path.\nThe origin_decision_ref 'null' sentinel (literal string \"null\") IS rejected here via not:{const:\"null\"}.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "type", "title", "summary"],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^C-[A-Z0-9]+(-[A-Z0-9]+)*$",
      "description": "Concept node identifier; matches RFC §2.2 pattern C-<SEGMENT>(-<SEGMENT>)*"
    },
    "type": {
      "enum": ["concept", "moc"],
      "description": "concept = RFC-0001 concept node; moc = D-15 Map of Content node"
    },
    "title": {
      "type": "string",
      "minLength": 1
    },
    "summary": {
      "type": "string",
      "minLength": 1,
      "description": "≤25 words enforced by spec-lint.mjs, not this schema"
    },
    "parent": {
      "oneOf": [
        { "type": "string", "minLength": 1 },
        { "type": "null" }
      ],
      "description": "Parent concept id, or null for the root concept (C-GROUNDWORK)"
    },
    "origin_decision_ref": {
      "type": "string",
      "minLength": 1,
      "not": { "const": "null" },
      "description": "Decision ref that introduced this node, in the form <motive-slug>#D-<n> (e.g. plugin-cleanup#D-5); literal string 'null' is rejected here"
    },
    "superseded_by": {
      "type": "string",
      "minLength": 1
    },
    "snapshot_of": {
      "type": "string",
      "minLength": 1
    },
    "status": {
      "type": "string"
    },
    "depends_on": {
      "oneOf": [
        { "type": "string" },
        { "type": "array", "items": { "type": "string" } }
      ],
      "description": "D-15 ConceptIndex: concepts this node depends on"
    },
    "date_updated": {
      "type": "string",
      "description": "D-15 ConceptIndex: last meaningful update date"
    },
    "tags": {
      "oneOf": [
        { "type": "string" },
        { "type": "array", "items": { "type": "string" } },
        { "type": "null" }
      ]
    },
    "aliases": {
      "oneOf": [
        { "type": "string" },
        { "type": "array", "items": { "type": "string" } },
        { "type": "null" }
      ]
    }
  }
}
